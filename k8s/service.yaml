apiVersion: v1
kind: Service
metadata:
  name: pronunt-ai-service
  namespace: prism
spec:
  selector:
    app: pronunt-ai-service
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  type: ClusterIP

